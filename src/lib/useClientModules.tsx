'use client';

import Map, {
  Source, Layer, Popup, MapLayerMouseEvent, LngLatBounds,
} from 'react-map-gl';

export default Map;
export {
  Source,
  Layer,
  Popup,
};
export type {
  MapLayerMouseEvent,
  LngLatBounds,
};
